<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        tools:context=".view.UserLoginActivity">

        <Button
            android:id="@+id/bt_login_close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="close"/>

        <EditText
            android:id="@+id/et_login_idInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:hint="@string/login_id_hint"
            android:inputType="textEmailAddress"
            android:maxLength="15"
            android:text="delivery" />

        <EditText
            android:id="@+id/et_login_pwInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:hint="@string/login_password_hint"
            android:inputType="textPassword"
            android:maxLength="15"
            android:text="dev_baemin" />

        <TextView
            android:id="@+id/tv_login_refresh"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:textColor="@color/main"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_login_access"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:textColor="@color/main"
            android:textStyle="bold" />

        <Button
            android:id="@+id/bt_login_login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:maxLength="6"
            android:text="@string/login" />
    </androidx.appcompat.widget.LinearLayoutCompat>
</layout>